# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Built containers (do NOT distribute)
*.sif

# Downloaded / extracted tool binaries (regenerated by build_images.sh; do NOT commit)
static/apptainer/salmon
static/apptainer/salmon-*/
static/apptainer/*.tar.gz

# Local data / secrets
*.sqlite
.env
*.key
